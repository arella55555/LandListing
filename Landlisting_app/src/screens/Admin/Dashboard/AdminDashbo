// DashboardScreen.tsx
// PERFECT MODERN HCI MOBILE ADMIN DASHBOARD
// LandListing Admin System
// Expo + React Native + TypeScript

// INSTALL:
// expo install react-native-safe-area-context
// expo install @expo/vector-icons

import React from "react";
import {
  View,
  Text,
  StyleSheet,
  ScrollView,
  TouchableOpacity,
  TextInput,
} from "react-native";

import { SafeAreaView } from "react-native-safe-area-context";

import {
  Feather,
  Ionicons,
  MaterialIcons,
  AntDesign,
} from "@expo/vector-icons";

export default function DashboardScreen() {
  return (
    <SafeAreaView style={styles.container}>
      <ScrollView
        showsVerticalScrollIndicator={false}
        contentContainerStyle={styles.scrollContent}
      >
        {/* ================================================= */}
        {/* HEADER */}
        {/* ================================================= */}

        <View style={styles.header}>
          <View>
            <Text style={styles.greeting}>Good Morning</Text>

            <Text style={styles.adminName}>
              LandListing Admin
            </Text>
          </View>

          <TouchableOpacity style={styles.notificationButton}>
            <Ionicons
              name="notifications-outline"
              size={24}
              color="#111827"
            />
          </TouchableOpacity>
        </View>

        {/* ================================================= */}
        {/* SEARCH */}
        {/* ================================================= */}

        <View style={styles.searchContainer}>
          <Feather name="search" size={18} color="#9CA3AF" />

          <TextInput
            placeholder="Search users, listings, reports..."
            placeholderTextColor="#9CA3AF"
            style={styles.searchInput}
          />
        </View>

        {/* ================================================= */}
        {/* QUICK OVERVIEW */}
        {/* ================================================= */}

        <Text style={styles.sectionTitle}>
          Platform Overview
        </Text>

        <View style={styles.statsGrid}>
          <View style={styles.statCard}>
            <View style={styles.iconPurple}>
              <Ionicons
                name="people-outline"
                size={22}
                color="#7C3AED"
              />
            </View>

            <Text style={styles.statNumber}>12,483</Text>

            <Text style={styles.statLabel}>
              Total Users
            </Text>
          </View>

          <View style={styles.statCard}>
            <View style={styles.iconBlue}>
              <Ionicons
                name="home-outline"
                size={22}
                color="#2563EB"
              />
            </View>

            <Text style={styles.statNumber}>1,248</Text>

            <Text style={styles.statLabel}>
              Active Listings
            </Text>
          </View>

          <View style={styles.statCard}>
            <View style={styles.iconOrange}>
              <Ionicons
                name="shield-checkmark-outline"
                size={22}
                color="#EA580C"
              />
            </View>

            <Text style={styles.statNumber}>18</Text>

            <Text style={styles.statLabel}>
              Pending Verification
            </Text>
          </View>

          <View style={styles.statCard}>
            <View style={styles.iconRed}>
              <MaterialIcons
                name="report-problem"
                size={22}
                color="#DC2626"
              />
            </View>

            <Text style={styles.statNumber}>9</Text>

            <Text style={styles.statLabel}>
              Flagged Listings
            </Text>
          </View>
        </View>

        {/* ================================================= */}
        {/* QUICK ACTIONS */}
        {/* ================================================= */}

        <Text style={styles.sectionTitle}>
          Quick Actions
        </Text>

        <View style={styles.quickActionsRow}>
          <TouchableOpacity style={styles.quickActionCard}>
            <View style={styles.quickActionIcon}>
              <Ionicons
                name="checkmark-circle-outline"
                size={24}
                color="#7C3AED"
              />
            </View>

            <Text style={styles.quickActionText}>
              Review Listings
            </Text>
          </TouchableOpacity>

          <TouchableOpacity style={styles.quickActionCard}>
            <View style={styles.quickActionIcon}>
              <Ionicons
                name="shield-outline"
                size={24}
                color="#7C3AED"
              />
            </View>

            <Text style={styles.quickActionText}>
              Verify Sellers
            </Text>
          </TouchableOpacity>
        </View>

        {/* ================================================= */}
        {/* PRIORITY ALERTS */}
        {/* ================================================= */}

        <Text style={styles.sectionTitle}>
          Priority Alerts
        </Text>

        <View style={styles.alertCard}>
          <View style={styles.alertLeft}>
            <View style={styles.alertIcon}>
              <MaterialIcons
                name="warning-amber"
                size={24}
                color="#DC2626"
              />
            </View>

            <View style={{ flex: 1 }}>
              <Text style={styles.alertTitle}>
                Suspicious Seller Activity
              </Text>

              <Text style={styles.alertDescription}>
                3 accounts triggered fraud detection
              </Text>
            </View>
          </View>

          <AntDesign
            name="right"
            size={16}
            color="#9CA3AF"
          />
        </View>

        <View style={styles.alertCard}>
          <View style={styles.alertLeft}>
            <View style={styles.alertIconPurple}>
              <Ionicons
                name="document-text-outline"
                size={22}
                color="#7C3AED"
              />
            </View>

            <View style={{ flex: 1 }}>
              <Text style={styles.alertTitle}>
                Verification Queue Growing
              </Text>

              <Text style={styles.alertDescription}>
                18 seller verifications pending
              </Text>
            </View>
          </View>

          <AntDesign
            name="right"
            size={16}
            color="#9CA3AF"
          />
        </View>

        {/* ================================================= */}
        {/* MODERATION QUEUE */}
        {/* ================================================= */}

        <Text style={styles.sectionTitle}>
          Moderation Queue
        </Text>

        <View style={styles.queueCard}>
          <View style={styles.queueHeader}>
            <Text style={styles.queueTitle}>
              Reported Listing
            </Text>

            <View style={styles.highPriorityBadge}>
              <Text style={styles.highPriorityText}>
                HIGH
              </Text>
            </View>
          </View>

          <Text style={styles.queueListing}>
            Beachfront Property — Cebu
          </Text>

          <Text style={styles.queueReason}>
            Multiple users reported fake ownership
          </Text>

          <View style={styles.queueButtons}>
            <TouchableOpacity
              style={styles.reviewButton}
            >
              <Text style={styles.reviewButtonText}>
                Review
              </Text>
            </TouchableOpacity>

            <TouchableOpacity
              style={styles.dismissButton}
            >
              <Text style={styles.dismissButtonText}>
                Dismiss
              </Text>
            </TouchableOpacity>
          </View>
        </View>

        {/* ================================================= */}
        {/* RECENT ACTIVITY */}
        {/* ================================================= */}

        <Text style={styles.sectionTitle}>
          Recent Activity
        </Text>

        {[
          "Admin approved a seller verification",
          "Listing removed for fraud",
          "Moderator resolved a report",
          "New flagged listing detected",
        ].map((item, index) => (
          <View key={index} style={styles.activityCard}>
            <View style={styles.activityDot} />

            <View style={{ flex: 1 }}>
              <Text style={styles.activityText}>
                {item}
              </Text>

              <Text style={styles.activityTime}>
                2 minutes ago
              </Text>
            </View>
          </View>
        ))}

        {/* ================================================= */}
        {/* ANALYTICS SNAPSHOT */}
        {/* ================================================= */}

        <Text style={styles.sectionTitle}>
          Weekly Analytics
        </Text>

        <View style={styles.analyticsCard}>
          <View style={styles.analyticsRow}>
            <Text style={styles.analyticsLabel}>
              Listings Approved
            </Text>

            <Text style={styles.analyticsValue}>
              324
            </Text>
          </View>

          <View style={styles.analyticsProgress}>
            <View
              style={[
                styles.analyticsFill,
                { width: "78%" },
              ]}
            />
          </View>

          <View style={styles.analyticsRow}>
            <Text style={styles.analyticsLabel}>
              Reports Resolved
            </Text>

            <Text style={styles.analyticsValue}>
              89%
            </Text>
          </View>

          <View style={styles.analyticsProgress}>
            <View
              style={[
                styles.analyticsFillPurple,
                { width: "89%" },
              ]}
            />
          </View>
        </View>

        {/* ================================================= */}
        {/* SYSTEM STATUS */}
        {/* ================================================= */}

        <Text style={styles.sectionTitle}>
          System Status
        </Text>

        <View style={styles.systemCard}>
          <View style={styles.systemRow}>
            <Text style={styles.systemLabel}>
              API Status
            </Text>

            <Text style={styles.systemGood}>
              Operational
            </Text>
          </View>

          <View style={styles.systemRow}>
            <Text style={styles.systemLabel}>
              Moderation Queue
            </Text>

            <Text style={styles.systemNormal}>
              Stable
            </Text>
          </View>

          <View style={styles.systemRow}>
            <Text style={styles.systemLabel}>
              Server Health
            </Text>

            <Text style={styles.systemGood}>
              Good
            </Text>
          </View>
        </View>

        {/* Bottom Spacing */}
        <View style={{ height: 120 }} />
      </ScrollView>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#F8FAFC",
  },

  scrollContent: {
    paddingHorizontal: 20,
    paddingTop: 12,
  },

  header: {
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
  },

  greeting: {
    fontSize: 14,
    color: "#6B7280",
  },

  adminName: {
    fontSize: 28,
    fontWeight: "700",
    color: "#111827",
    marginTop: 4,
  },

  notificationButton: {
    backgroundColor: "#FFFFFF",
    width: 52,
    height: 52,
    borderRadius: 18,
    justifyContent: "center",
    alignItems: "center",
  },

  searchContainer: {
    marginTop: 22,
    backgroundColor: "#FFFFFF",
    borderRadius: 20,
    flexDirection: "row",
    alignItems: "center",
    paddingHorizontal: 16,
    paddingVertical: 14,
  },

  searchInput: {
    marginLeft: 10,
    flex: 1,
    fontSize: 15,
    color: "#111827",
  },

  sectionTitle: {
    marginTop: 30,
    marginBottom: 16,
    fontSize: 18,
    fontWeight: "700",
    color: "#111827",
  },

  statsGrid: {
    flexDirection: "row",
    flexWrap: "wrap",
    justifyContent: "space-between",
  },

  statCard: {
    width: "48%",
    backgroundColor: "#FFFFFF",
    borderRadius: 28,
    padding: 20,
    marginBottom: 16,
  },

  iconPurple: {
    width: 48,
    height: 48,
    borderRadius: 14,
    backgroundColor: "#F3E8FF",
    justifyContent: "center",
    alignItems: "center",
  },

  iconBlue: {
    width: 48,
    height: 48,
    borderRadius: 14,
    backgroundColor: "#DBEAFE",
    justifyContent: "center",
    alignItems: "center",
  },

  iconOrange: {
    width: 48,
    height: 48,
    borderRadius: 14,
    backgroundColor: "#FFEDD5",
    justifyContent: "center",
    alignItems: "center",
  },

  iconRed: {
    width: 48,
    height: 48,
    borderRadius: 14,
    backgroundColor: "#FEE2E2",
    justifyContent: "center",
    alignItems: "center",
  },

  statNumber: {
    marginTop: 18,
    fontSize: 26,
    fontWeight: "700",
    color: "#111827",
  },

  statLabel: {
    marginTop: 8,
    fontSize: 14,
    color: "#6B7280",
  },

  quickActionsRow: {
    flexDirection: "row",
    justifyContent: "space-between",
  },

  quickActionCard: {
    width: "48%",
    backgroundColor: "#FFFFFF",
    borderRadius: 24,
    paddingVertical: 24,
    alignItems: "center",
  },

  quickActionIcon: {
    width: 56,
    height: 56,
    borderRadius: 18,
    backgroundColor: "#F3E8FF",
    justifyContent: "center",
    alignItems: "center",
  },

  quickActionText: {
    marginTop: 12,
    fontWeight: "600",
    color: "#111827",
  },

  alertCard: {
    backgroundColor: "#FFFFFF",
    borderRadius: 24,
    padding: 18,
    marginBottom: 14,
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
  },

  alertLeft: {
    flexDirection: "row",
    alignItems: "center",
    flex: 1,
  },

  alertIcon: {
    width: 52,
    height: 52,
    borderRadius: 16,
    backgroundColor: "#FEE2E2",
    justifyContent: "center",
    alignItems: "center",
    marginRight: 14,
  },

  alertIconPurple: {
    width: 52,
    height: 52,
    borderRadius: 16,
    backgroundColor: "#F3E8FF",
    justifyContent: "center",
    alignItems: "center",
    marginRight: 14,
  },

  alertTitle: {
    fontWeight: "700",
    color: "#111827",
  },

  alertDescription: {
    marginTop: 4,
    color: "#6B7280",
    fontSize: 13,
  },

  queueCard: {
    backgroundColor: "#FFFFFF",
    borderRadius: 28,
    padding: 20,
  },

  queueHeader: {
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
  },

  queueTitle: {
    fontSize: 17,
    fontWeight: "700",
    color: "#111827",
  },

  highPriorityBadge: {
    backgroundColor: "#FEE2E2",
    paddingHorizontal: 12,
    paddingVertical: 6,
    borderRadius: 20,
  },

  highPriorityText: {
    color: "#DC2626",
    fontWeight: "700",
    fontSize: 12,
  },

  queueListing: {
    marginTop: 14,
    fontWeight: "600",
    color: "#111827",
  },

  queueReason: {
    marginTop: 8,
    color: "#6B7280",
    lineHeight: 20,
  },

  queueButtons: {
    flexDirection: "row",
    marginTop: 20,
  },

  reviewButton: {
    flex: 1,
    backgroundColor: "#7C3AED",
    paddingVertical: 14,
    borderRadius: 16,
    alignItems: "center",
    marginRight: 10,
  },

  reviewButtonText: {
    color: "#FFFFFF",
    fontWeight: "700",
  },

  dismissButton: {
    flex: 1,
    backgroundColor: "#F3F4F6",
    paddingVertical: 14,
    borderRadius: 16,
    alignItems: "center",
  },

  dismissButtonText: {
    color: "#111827",
    fontWeight: "700",
  },

  activityCard: {
    backgroundColor: "#FFFFFF",
    borderRadius: 22,
    padding: 18,
    flexDirection: "row",
    alignItems: "center",
    marginBottom: 12,
  },

  activityDot: {
    width: 12,
    height: 12,
    borderRadius: 6,
    backgroundColor: "#7C3AED",
    marginRight: 14,
  },

  activityText: {
    fontWeight: "600",
    color: "#111827",
  },

  activityTime: {
    marginTop: 6,
    color: "#6B7280",
    fontSize: 13,
  },

  analyticsCard: {
    backgroundColor: "#FFFFFF",
    borderRadius: 28,
    padding: 20,
  },

  analyticsRow: {
    flexDirection: "row",
    justifyContent: "space-between",
    marginBottom: 10,
  },

  analyticsLabel: {
    color: "#111827",
    fontWeight: "600",
  },

  analyticsValue: {
    color: "#7C3AED",
    fontWeight: "700",
  },

  analyticsProgress: {
    height: 10,
    backgroundColor: "#E5E7EB",
    borderRadius: 20,
    overflow: "hidden",
    marginBottom: 20,
  },

  analyticsFill: {
    height: "100%",
    backgroundColor: "#2563EB",
    borderRadius: 20,
  },

  analyticsFillPurple: {
    height: "100%",
    backgroundColor: "#7C3AED",
    borderRadius: 20,
  },

  systemCard: {
    backgroundColor: "#FFFFFF",
    borderRadius: 28,
    padding: 20,
  },

  systemRow: {
    flexDirection: "row",
    justifyContent: "space-between",
    marginBottom: 18,
  },

  systemLabel: {
    color: "#111827",
    fontWeight: "600",
  },

  systemGood: {
    color: "#16A34A",
    fontWeight: "700",
  },

  systemNormal: {
    color: "#EA580C",
    fontWeight: "700",
  },
});